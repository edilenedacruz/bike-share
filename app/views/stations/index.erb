<!DOCTYPE html>
 <html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Bike-Share</title>
  </head>
  <body>
    <h1>All Stations<h1>
    <hr>
    <button type="button"><a href="/stations/new">Add New Station</a></button>
    <table>
      <tr>
        <th>Name</th>
        <th>Additional Info</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
        <% @stations.each do |station| %>
          <tr>
            <td><%= station.name %></td>
            <td><button type='button'><a href="/stations/<%=station.id %>">Additional Info</a></button></td>
            <td><button><a href="/stations/<%=station.id %>/edit">Edit</a></button></td>
            <td>
              <form action="/stations/<%= station.id %>" method="post">
                <input type="hidden" name="_method" value="DELETE" />
                <input type="submit" value="Delete" />
              </form>
            </td>
          </tr>
      <% end %>
    </table>
    <hr>
      <%= will_paginate @stations %>

  </body>
 </html>